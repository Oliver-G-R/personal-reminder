import { Tcolor } from './TColorTheme'

interface IReminderData {
    titile: string
    fullReminder: string
    date: Date
    time: Date
    color: Tcolor
    id:string
}

export {
  IReminderData
}
